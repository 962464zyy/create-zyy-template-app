import React from 'react';
    
export interface IndexProps {};
const index = (props: IndexProps) => {
    
    return (
        <div>
            test
        </div>
    );
};
    
export default index;